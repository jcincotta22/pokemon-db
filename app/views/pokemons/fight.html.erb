
<div class="form-title">
  <h2>Pokemon Battle Analyzer</h2>
  <p>Input the pokemon you are going to fight.</p>
  <p>This form will then select the best pokemon to use from your current collection for your upcoming battle.</p>
</div>

  <div class="form">
    <%= form_for @fight, url: {action: "fighting"}, html: {class: "form"} do |f| %>
    <div class="input-group select">
      <%= f.select :name, options_for_select(@pokemon_names_collection), class: "input-group" %>
    </div>
      <div class="input-group-button">
        <%= f.submit "Battle", class: "button" %>
        <% end %>
      </div>
  </div>
